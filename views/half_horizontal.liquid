<style>
  .screen { align-items: flex-start !important; justify-content: flex-start !important; }
  .view--half_horizontal .msm-header { display: flex; justify-content: space-between; align-items: baseline; padding: 8px 16px 4px; width: 100%; box-sizing: border-box; }
  .view--half_horizontal .msm-menu-label { padding: 2px 16px 0; width: 100%; box-sizing: border-box; }
  .view--half_horizontal .msm-days { display: flex; gap: 6px; padding: 0 16px; width: 100%; box-sizing: border-box; overflow: hidden; }
  .view--half_horizontal .msm-day { flex: 1; min-width: 0; }
  .view--half_horizontal .msm-today-cols { display: flex; gap: 12px; padding: 0 16px; width: 100%; box-sizing: border-box; overflow: hidden; flex: 1; }
  .view--half_horizontal .msm-today-col { flex: 1; min-width: 0; }
</style>
<div class="view view--half_horizontal">
  <div class="msm-header">
    <span class="title">{% if display == "week" %}This Week's {% endif %}School Menus</span>
    <span class="label">{{ date }}</span>
  </div>
  {% if display == "today" %}
    <div class="msm-today-cols">
      {% for menu in menus %}
      <div class="msm-today-col">
        <div class="label label--underline">{{ menu.site_name }} — {{ menu.menu_name }}</div>
        {% assign day = menu.days[0] %}
        {% if day.notice %}
          <div style="font-size: 14px; font-style: italic;">{{ day.notice }}</div>
        {% elsif day.items.size > 0 %}
          {% for item in day.items %}
          <div style="font-size: 14px;">{{ item }}</div>
          {% endfor %}
        {% else %}
          <div>No menu available</div>
        {% endif %}
      </div>
      {% endfor %}
    </div>
  {% else %}
    {% for menu in menus %}
    <div class="msm-menu-label">
      <span class="label label--underline">{{ menu.site_name }} — {{ menu.menu_name }}</span>
    </div>
    <div class="msm-days">
      {% for day in menu.days %}
      <div class="msm-day">
        <div class="label label--underline">{{ day.date_short }}</div>
        {% if day.notice %}
          <div style="font-size: 12px; font-style: italic;">{{ day.notice }}</div>
        {% elsif day.items.size > 0 %}
          {% for item in day.items %}
          <div style="font-size: 12px;">{{ item }}</div>
          {% endfor %}
        {% else %}
          <div style="font-size: 12px;">—</div>
        {% endif %}
      </div>
      {% endfor %}
    </div>
    {% endfor %}
  {% endif %}
</div>

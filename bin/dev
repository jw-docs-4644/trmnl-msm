#!/usr/bin/env python3
"""Start the local TRMNL preview server."""

import subprocess
import sys
from pathlib import Path

PROJECT_DIR = Path(__file__).resolve().parent.parent

result = subprocess.run(["trmnlp", "serve", str(PROJECT_DIR)])
sys.exit(result.returncode)
